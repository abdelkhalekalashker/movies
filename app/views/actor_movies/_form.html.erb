<%= form_with(model: actor_movie) do |form| %>
  <% if actor_movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(actor_movie.errors.count, "error") %> prohibited this actor_movie from being saved:</h2>

      <ul>
        <% actor_movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :actor_id, style: "display: block" %>
    <%= form.text_field :actor_id %>
  </div>

  <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.text_field :movie_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
