<%= form_with(model: city_movie) do |form| %>
  <% if city_movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(city_movie.errors.count, "error") %> prohibited this city_movie from being saved:</h2>

      <ul>
        <% city_movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :city_id, style: "display: block" %>
    <%= form.text_field :city_id %>
  </div>

  <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.text_field :movie_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
