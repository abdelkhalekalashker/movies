<%= form_with(model: director_movie) do |form| %>
  <% if director_movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(director_movie.errors.count, "error") %> prohibited this director_movie from being saved:</h2>

      <ul>
        <% director_movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :director_id, style: "display: block" %>
    <%= form.text_field :director_id %>
  </div>

  <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.text_field :movie_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
